#ifndef RANDOM_H
#define RANDOM_H

//Rpi has inbuilt random hardware
// don't have time to dev that, so let's just borrow someone else's

unsigned rand(unsigned long long* seed);

#endif //RANDOM_H
